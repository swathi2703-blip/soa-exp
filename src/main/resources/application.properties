spring.application.name=config-server
# A single endpoint that all microservices contact for external configuration.
server.port=${PORT:8888}

# Keep configuration in a repository packaged with this server. For production,
# replace the native profile with the Git backend and use a protected config repo.
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=classpath:/config
